# MMZR Family Office - Dependencias do Sistema
# Versao 1.0.0 - Compativel com Python 3.8+

# Analise e manipulacao de dados
pandas>=2.0.0,<3.0.0

# Leitura e escrita de arquivos Excel
openpyxl>=3.1.0,<4.0.0

# Processamento de datas e tempo
python-dateutil>=2.8.0,<3.0.0

# Integracao com Windows/Outlook (Windows apenas)
pywin32>=306; sys_platform == "win32"

# Interface gráfica (incluso no Python, mas listado para referência)
# tkinter - Biblioteca padrão do Python

# Logging e utilitários (bibliotecas padrão, listadas para documentação)
# logging - Biblioteca padrão do Python
# os - Biblioteca padrão do Python
# sys - Biblioteca padrão do Python
# datetime - Biblioteca padrão do Python
# typing - Biblioteca padrão do Python (Python 3.5+)
# threading - Biblioteca padrão do Python
# base64 - Biblioteca padrão do Python

# Dependências de desenvolvimento (opcional)
# pytest>=7.0.0 - Para testes unitários
# black>=22.0.0 - Para formatação de código
# flake8>=4.0.0 - Para linting 